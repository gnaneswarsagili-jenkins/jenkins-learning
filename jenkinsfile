pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Building application from GitHub repository'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application'
            }
        }

    }
}
